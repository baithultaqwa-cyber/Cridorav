# Optional: deploy frontend as a second Railway service (Root directory: `frontend`)
[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npx --yes serve@14 -s dist -l $PORT"
restartPolicyType = "ON_FAILURE"
